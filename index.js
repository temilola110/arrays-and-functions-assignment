// What are the differences between mutating array methods and non-mutation array methods in JavaScript. List 5 array methods that fall under each of them
// 1)Mutating methods are ones that change the object after the method has been used. examples are:
        // array.fill()
        // array.pop() 
        // array.push()
        // array.reverse()
        // array.shift()

        
// 2)Non-mutating methods do not change the object after the method has been used.  examples are:
    //  array.concat()
    //  array.includes()
    //  array.join()
    //  array.lastIndexOf
    //  array.slice()